diff --git a/feeds/istore/luci/luci-app-store/Makefile b/feeds/istore/luci/luci-app-store/Makefile
--- a/feeds/istore/luci/luci-app-store/Makefile
+++ b/feeds/istore/luci/luci-app-store/Makefile
@@ -11,9 +11,8 @@ LUCI_DEPENDS:=+curl +opkg +luci-lib-ipkg +tar +libuci-lua +mount-utils +luci-lib
 LUCI_EXTRA_DEPENDS:=luci-lib-taskd (>=1.0.19)
 LUCI_PKGARCH:=all
 
-PKG_VERSION:=0.1.32-1
-# PKG_RELEASE MUST be empty for luci.mk
-PKG_RELEASE:=
+PKG_VERSION:=0.1.32
+PKG_RELEASE:=1
 
 ISTORE_UI_VERSION:=0.1.32
 ISTORE_UI_RELEASE:=1
@@ -25,7 +24,7 @@ PKG_SOURCE_URL:=https://github.com/linkease/istore-ui/archive/refs/tags
 
 PKG_MAINTAINER:=jjm2473 <jjm2473@gmail.com>
 
-TARGET_CONFIGURE_OPTS= FRONTEND_DIST="$(BUILD_DIR)/istore-ui-$(ISTORE_UI_VERSION)-$(ISTORE_UI_RELEASE)/app-store-ui/src/dist" APP_STORE_VERSION="$(PKG_VERSION)"
+TARGET_CONFIGURE_OPTS= FRONTEND_DIST="$(BUILD_DIR)/istore-ui-$(ISTORE_UI_VERSION)-$(ISTORE_UI_RELEASE)/app-store-ui/src/dist" APP_STORE_VERSION="$(PKG_VERSION)-$(PKG_RELEASE)"
 TARGET_CONFIGURE_OPTS+= SED="$(SED)"
 
 define Package/luci-app-store/conffiles
